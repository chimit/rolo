const config = {}

config.port = 3000

config.mysql = {
    connectionLimit: 10,
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'tasks',
}

config.redis = {
    host: 'localhost',
    port: 6379,
}

module.exports = config
